# CLAUDE.md

このファイルは Claude Code がセッション開始時に自動で読み込むプロジェクト規約である.

## プロジェクト概要

応用情報技術者試験および大学講義の試験対策に用いる, 個人用の単語帳アプリ ( PWA ). Quizlet の中核機能を実装対象とする.

**`specs.md` が仕様の正本である.** 実装の判断に迷った場合は必ず `specs.md` を参照すること. 仕様と実装が食い違った場合は, どちらを正とするかを利用者に確認する. 仕様変更が決まった場合は `specs.md` を先に更新してから実装する.

## 対話の方針

- **日本語で応答する.**
- **git および GitHub の操作を行う際は, 実行するコマンドとその意味を先に日本語で説明してから実行する.** 利用者は GitHub の学習を兼ねているため, 操作を黙って進めない.
- GitHub の Web 画面上で利用者自身が行う必要がある操作 ( リポジトリ設定など ) が生じた場合は, 画面上のどのメニューを辿るかを具体的に案内する.
- 利用者は情報通信工学科の3年生である. プログラミングの基礎知識はあるが, Web フロントエンドと git の実務経験は浅い前提で説明する.

## 技術構成

| 層 | 採用 |
|---|---|
| 言語 | TypeScript ( strict モード ) |
| UI | React |
| ビルド | Vite |
| DB | Dexie.js ( IndexedDB ) |
| 状態管理 | Zustand |
| ルーティング | React Router の **HashRouter** |
| 数式描画 | KaTeX |
| ZIP 入出力 | fflate |
| PWA 化 | vite-plugin-pwa |
| 公開 | GitHub Pages + GitHub Actions |

## 公開設定 ( 重要 )

GitHub Pages のプロジェクトサイトは `https://<ユーザ名>.github.io/<リポジトリ名>/` で公開される. サブディレクトリ配下になるため, **`vite.config.ts` の `base` をリポジトリ名に合わせて設定しなければならない.** これを怠ると, 公開後に JavaScript と CSS の読み込みがすべて404になる.

```ts
// vite.config.ts
export default defineConfig({
  base: '/flashcard-app/',   // ← リポジトリ名と一致させる
})
```

ルーティングに HashRouter を用いるのも同じ理由による. GitHub Pages はサーバ側の書き換え設定を持たないため, BrowserRouter を使うとページの再読み込みで404になる.

## ディレクトリ構成

ロジック層を UI から分離する. 将来 React Native へ移行する可能性を残すためであり, テストも書きやすくなる.

```
src/
  core/              # UI に依存しないロジック層. React を import しない
    db/              # Dexie のスキーマ定義とデータアクセス
    study/           # キュー構築, 正誤判定, 4択の選択肢生成
    import/          # テキストインポートの解析
    search/          # 検索用の正規化と照合
    media/           # 画像のリサイズと圧縮
    backup/          # ZIP / JSON の書き出しと読み込み
    types.ts         # データモデルの型定義
  ui/
    screens/         # 画面単位のコンポーネント ( specs.md の S1〜S10 に対応 )
    components/      # 再利用するコンポーネント
    hooks/
  App.tsx
  main.tsx
public/
```

`src/core/` の中では React を import しない. この境界を守ること.

## コーディング規約

- TypeScript は `strict: true`. `any` を使わない. 型が定まらない場合は `unknown` を使って絞り込む.
- データモデルの型は `src/core/types.ts` に集約し, 各所で再定義しない.
- ファイル名は, コンポーネントは PascalCase ( `CardEditor.tsx` ), それ以外は camelCase ( `buildQueue.ts` ).
- 日付は UNIX ミリ秒の `number` で保持する. `Date` オブジェクトを IndexedDB に格納しない.
- ID は `crypto.randomUUID()` で生成する.
- コメントは日本語で書いてよい. ただし「何をしているか」ではなく「なぜそうしているか」を書く.

## 実装上の要注意点

`specs.md` に根拠を記した箇所のうち, 誤りやすいものを再掲する.

1. **テキストインポートの分割** — 用語と定義の区切り文字が1行に複数出現する場合, **最初の1つでのみ分割する**. 定義側に「機密性, 完全性, 可用性」のようなカンマを含む用語が頻出するため.

2. **検索の正規化順序** — NFKC 正規化 → 小文字化 → カタカナのひらがな化, の順で適用する. NFKC を先に行わないと半角カナが拾えない. U+30F7〜U+30FA ( ヷヸヹヺ ) には対応するひらがながないため変換対象から除外する. 正規化済み文字列はカード保存時に生成して保持し, 検索のたびに再計算しない.

3. **画像は取り込み時の圧縮を必須とする** — 無圧縮のまま IndexedDB に入れない. canvas で長辺1600pxへ縮小し WebP へ再符号化する. WebP 非対応環境では JPEG にフォールバックする.

4. **画像は Blob で保存する** — Base64 文字列で保存しない. 表示時は `createObjectURL` で展開し, **必ず `revokeObjectURL` で解放する.** 解放漏れはメモリリークになる.

5. **KaTeX は `throwOnError: false`** — 学習中に構文誤りで画面が停止することを防ぐ.

6. **数式は有効なセットでのみ解析する** — 無効なセットで `$` をパースしない. 金額表記などの誤爆を防ぐため.

7. **リッチコンテンツを無効化してもデータを消さない** — 表示のみをプレーンに戻す.

8. **学習データを localStorage に置かない** — 容量が小さく Blob を扱えない. IndexedDB を使う. localStorage はテーマ設定など軽微な値に限る.

## やってはいけないこと

- `node_modules/` や `dist/` をコミットしない ( `.gitignore` で除外済み ).
- プロジェクトを OneDrive の同期対象フォルダに置かない. `node_modules` の同期でビルドが破綻する.
- `main` ブランチへ直接 push する運用でよい ( 個人開発のため ). ただし各段階の区切りでは意味のある単位でコミットする.
- 仕様に定めのない機能を勝手に追加しない. 必要と判断した場合は提案し, 承諾を得てから `specs.md` を更新する.

## コミットメッセージ

Conventional Commits に従う. 本文は日本語でよい.

```
feat: 暗記モードのキュー構築を実装
fix: 検索でカタカナが拾えない問題を修正
docs: specs.md に画像圧縮の仕様を追記
chore: 依存パッケージを更新
refactor: core/study のキュー生成を関数に切り出す
test: インポート解析のテストを追加
```

## 開発の進め方

`specs.md` の §8 に段階0〜7を定義してある. 原則としてこの順に進め, 各段階の完了条件を満たしてからコミットする.

段階3 ( 暗記モードと進捗管理 ) を終えた時点で実用可能になるため, そこで一度実際の試験勉強に投入して使用感を確認する.
