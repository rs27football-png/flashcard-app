# 単語帳アプリ 仕様書 v3.9

最終更新: 2026-09-10

## 変更履歴

| 版 | 変更点 |
|---|---|
| v1 | 初版 (機能の箇条書き) |
| v2 | 実行形態・保存方式・進捗方式を確定. データモデル, アルゴリズム, 画面構成を定義 |
| v3 | 画像・数式の添付を追加 (学習セット単位で有効化). 公開先を GitHub Pages に確定. 4択の習得判定を1回正答に確定. 検索でひらがなとカタカナを同一視する仕様に変更 |
| v3.1 | `Card` に検索用の正規化済み文字列 `normalized` を追加 (6.3 の性能要件をデータモデル側に明記したもの) |
| v3.2 | テキストインポートの重複除外が, 貼り付けたテキスト内の重複も対象とすることを 4.5 に明記 |
| v3.3 | `StudySet` に `studyOptions` を追加 (2.7 の「セットごとに記憶する」の保存先を明示したもの) |
| v3.4 | 段階3 までを実際に使用した結果を反映. ホーム画面の直接操作, インポートの導線, 暗記モード中の設定変更, カードの重なり表示, アイコンの併用を追記 |
| v3.5 | パンくずリストの中間フォルダから, その場で配下のフォルダ・学習セットを辿って移動できることを 4.1 に追記 |
| v3.6 | 4択モードの出題オプション `QuizOptions` を定義し `StudySet.quizOptions` に記憶. 答えが空のカードの除外と, 4択の中断状態を保存しないことを明記 |
| v3.7 | セット操作の入口 (セット詳細とホームの ⋯), フォルダのコピー, 統合する順番の指定, 分割で空になった元セットの削除, 検索結果の表示件数を追記 |
| v3.8 | 統合の並べ替えをドラッグに変更. 統合元のセットを削除するかどうかは, 統合を実行したあとに尋ねる |
| v3.9 | 「編集」と「設定」の役割を整理. セットの名称・説明はカード編集画面へ, 名称変更・移動・削除は ⋯ メニューへ移した. アプリ内の句読点を「、」「。」に統一 |
| v3.10 | 「戻る」は行き先を決め打ちにせず, 直前に見ていた画面へ帰ることを 6.4 に明記 |
| v3.11 | 段階6 の実装にあたり, 画像の貼り付け先の決め方, 4択の選択肢に添える画像, KaTeX の読み込み時機を 4.4 に追記 |
| v3.12 | 段階7 の実装にあたり, 更新の適用方法, 先読みする字形の範囲, 取り込み時の設定と中断状態の扱いを 4.11 と 6.1 に追記 |

---

## 0. 目的と前提

### 0.1 目的

応用情報技術者試験および大学講義の試験対策を目的とした, 個人用の単語帳アプリを作成する. 参照モデルは Quizlet だが, 実装対象はその中核機能に限定し, ソーシャル機能・課金機能・高度な学習アルゴリズムは持たない.

### 0.2 確定した前提

| 項目 | 決定内容 |
|---|---|
| 実行形態 | PWA (Progressive Web App). ブラウザで動作し, iOS では「ホーム画面に追加」でアプリとして起動する |
| 対応環境 | iOS Safari 16.4 以降, デスクトップ版 Chrome / Edge / Safari |
| 公開先 | GitHub Pages (無料, HTTPS 提供. ソース管理を兼ねる) |
| データ保存 | 端末内 (IndexedDB) のみ. サーバは持たない |
| 端末間の移行 | ZIP または JSON ファイルの手動エクスポート / インポート |
| 学習進捗 | 学習セット単位で永続保存する (Quizlet 相当の循環方式) |
| リッチコンテンツ | 画像添付と数式 (LaTeX) に対応する. **学習セットごとに個別に有効化する** |
| 追加実装 | ヒント欄, 全セット横断検索 |
| 実装しない | 読み上げ音声 (TTS), クラウド同期, アカウント機能 |

### 0.3 この構成を選ぶ理由

PWA は Mac も Apple Developer Program (年99ドル) も不要で, 単一のコードベースが Windows PC と iPhone の両方で動作する. 本アプリの要件には端末固有機能 (カメラ, プッシュ通知, バックグラウンド処理) が含まれないため, ネイティブアプリとの機能差はほぼ生じない.

データを端末内に閉じることで, サーバ費用・認証実装・同期競合の解決がすべて不要になる. 利用者が単独であるため, 同期の必要性は「PC で作成したセットを iPhone に移す」場面に限られ, これはファイルの受け渡しで十分に満たせる.

---

## 1. 用語定義

| 用語 | 定義 |
|---|---|
| カード | 用語 (表) と定義 (裏) の対からなる最小単位 |
| 学習セット | カードのまとまり. 学習の実行単位 |
| フォルダ | 学習セットおよび下位フォルダを格納する入れ物. 階層の深さに制限を設けない |
| 暗記モード | カードを1枚ずつ提示し, 既知 / 未習に振り分ける学習形式 |
| 4択モード | 表面を問題文とし, 4つの選択肢から正答を選ぶ学習形式 |
| 進捗状態 | 各カードが持つ `未学習` / `学習中` / `習得済` の3値 |
| ラウンド | 暗記モードにおける, キューを1周する単位 |
| リッチコンテンツ | 画像添付および数式表示の総称. セット単位で有効・無効を切り替える |

---

## 2. データモデル

すべて IndexedDB に保存する. 各テーブルの構造を以下に定める.

### 2.1 Folder (フォルダ)

```
Folder {
  id:        string     // UUID v4
  name:      string     // 1〜100文字, 同一階層内での重複を許容する
  parentId:  string | null   // null はルート直下を意味する
  order:     number     // 同一階層内の表示順
  createdAt: number     // UNIX ミリ秒
  updatedAt: number
}
```

制約: `parentId` の連鎖が自己を含んではならない. フォルダ移動時に循環参照の検査を行い, 自身または自身の子孫を移動先に指定した場合は操作を拒否する.

### 2.2 StudySet (学習セット)

```
StudySet {
  id:          string
  name:        string          // 1〜100文字
  description: string          // 0〜500文字, 任意
  folderId:    string | null   // null はルート直下
  order:       number

  // リッチコンテンツの有効化フラグ (v3 で追加)
  enableImages: boolean        // 既定 false
  enableMath:   boolean        // 既定 false

  // 最後に使用した学習オプション (v3.3 で追加)
  studyOptions: StudyOptions   // 2.7 参照

  // 最後に使用した4択モードの出題オプション (v3.6 で追加)
  quizOptions:  QuizOptions    // 2.7 参照

  createdAt:   number
  updatedAt:   number
}
```

`enableImages` と `enableMath` は独立して切り替える. 画像だけを使うセット, 数式だけを使うセットのいずれも構成できる.

`studyOptions` は 2.7 に定める学習オプションを, 当該セットで最後に使用した状態のまま保持する. セットに属する値であるため独立したテーブルを設けず, セットの削除で自然に破棄され, バックアップでもセットとともに移送される.

`quizOptions` は4択モード用の出題オプションで, 暗記モードの `studyOptions` とは別に記憶する. 出題順の既定が異なる (4択は既定でシャッフル) うえ, 「学習中のカードのみ」が4択に固有の選択肢であるため, 共用すると一方の変更がもう一方に波及する.

### 2.3 Card (カード)

```
Card {
  id:                string
  setId:             string
  term:              string    // 用語 (表). 1〜1000文字
  definition:        string    // 定義 (裏). 1〜2000文字
  hint:              string    // ヒント. 0〜200文字, 任意

  // 画像参照 (v3 で追加). 各面に最大1枚
  termImageId:       string | null
  definitionImageId: string | null

  starred:           boolean   // ★マーキング
  order:             number    // セット内の表示順

  // 検索用の正規化済み文字列 (v3.1 で追加)
  normalized:        string    // 用語・定義・ヒントを連結し 4.10 の正規化を適用したもの

  createdAt:         number
  updatedAt:         number
}
```

画像のみのカードを許容するため, `termImageId` が設定されている場合に限り `term` の空文字列を認める. `definition` も同様とする.

`normalized` は用語・定義・ヒントを改行で連結し, 4.10 に定める正規化 (NFKC → 小文字化 → カタカナのひらがな化) を適用した文字列である. **カードの保存時に生成して保持し, 検索のたびに再計算しない** (6.3 の性能要件). 横断検索はまずこの列で絞り込み, 一致箇所の強調表示は絞り込んだカードに対してのみ各項目を個別に照合して行う. 派生値であるため, バックアップの JSON には含めず, 取り込み時に再生成する.

### 2.4 Asset (画像)

```
Asset {
  id:        string
  setId:     string     // 削除の連動と容量集計に用いる
  blob:      Blob       // 圧縮済みの画像本体
  mimeType:  string     // 'image/webp' または 'image/jpeg'
  width:     number     // 圧縮後のピクセル幅
  height:    number     // 圧縮後のピクセル高
  bytes:     number     // 圧縮後のバイト数
  createdAt: number
}
```

Base64 文字列ではなく Blob として格納する. Base64 はバイト数が約1.37倍に膨らみ, 読み書きのたびに符号変換が発生するため採用しない.

表示時は `URL.createObjectURL()` で一時 URL を生成し, 画面の破棄時に `URL.revokeObjectURL()` で確実に解放する.

### 2.5 CardProgress (カード進捗)

```
CardProgress {
  cardId:         string    // 主キー
  setId:          string    // 索引用に冗長保持
  status:         'unseen' | 'learning' | 'known'
  lastAnsweredAt: number | null
  quizCorrect:    number    // 4択モードでの累計正答数
  quizWrong:      number    // 4択モードでの累計誤答数
}
```

カード作成時に `status: 'unseen'` のレコードを同時に生成する. カード削除時に連動して削除する.

### 2.6 StudySession (中断状態の保存)

学習の途中でアプリを閉じた場合に再開できるよう, セットごとに1件だけ保持する.

```
StudySession {
  setId:        string    // 主キー
  mode:         'flashcard' | 'quiz'
  options:      StudyOptions
  queue:        string[]       // 出題順のカードID配列
  currentIndex: number
  roundNumber:  number
  updatedAt:    number
}
```

学習完了時, または「最初からやり直す」実行時に削除する.

4択モードの中断状態は現時点では保存しない. 結果画面に出す誤答の一覧 (4.7.4) を復元するには解答履歴の保持が別途必要になるためである. 解答ごとの進捗 (4.7.3) はその都度保存されるため, 途中で閉じても失われない.

### 2.7 StudyOptions (学習オプション)

```
StudyOptions {
  trackProgress: boolean            // 進み具合を把握. 既定 true
  starredOnly:   boolean            // ★のみ. 既定 false
  front:         'term' | 'definition'   // 表面. 既定 'term'
  shuffle:       boolean            // シャッフル. 既定 false
}
```

セットごとに最後に使用した値を記憶し, 次回の既定値とする. 保存先は `StudySet.studyOptions` である (2.2 参照).

4択モードでは次の `QuizOptions` を用いる. 同じくセットごとに記憶し, 保存先は `StudySet.quizOptions` である.

```
QuizOptions {
  starredOnly:  boolean                 // ★のみ. 既定 false
  learningOnly: boolean                 // 学習中のカードのみ. 既定 false
  front:        'term' | 'definition'   // 問題文にする側. 既定 'term'
  shuffle:      boolean                 // 出題順のシャッフル. 既定 true (4.7.1)
}
```

### 2.8 AppSettings (アプリ設定)

```
AppSettings {
  theme:               'dark' | 'light' | 'system'   // 既定 'system'
  showShortcutHints:   boolean    // 既定 true
  quizAffectsProgress: boolean    // 4択の結果を進捗に反映. 既定 true
  imageMaxEdge:        number     // 画像リサイズの長辺上限. 既定 1600
  lastBackupAt:        number | null
}
```

---

## 3. 画面構成

| # | 画面 | 主な内容 |
|---|---|---|
| S1 | ホーム | フォルダツリーと学習セット一覧, 新規作成, 検索への入口 |
| S2 | セット詳細 | カード一覧, 進捗サマリ, 学習開始ボタン, セット操作メニュー |
| S3 | カード編集 | セット名・説明の編集, 用語・定義・ヒント・画像の入力, 追加 / 更新 / 削除, 並べ替え |
| S4 | インポート | テキスト貼り付け, 区切り文字指定, プレビュー |
| S5 | 暗記モード | カード提示と左右振り分け |
| S6 | 4択モード | 問題文と4つの選択肢 |
| S7 | 結果 | ラウンド結果, 誤答一覧, 次アクション |
| S8 | 検索 | 全セット横断の全文検索と結果一覧 |
| S9 | 設定 | テーマ, バックアップ, 使用容量, 各種既定値 |
| S10 | セット設定 | リッチコンテンツの有効化. 段階6 で追加する |

画面遷移は S1 を起点とし, S5 / S6 は S2 から起動して S7 で終端する.

---

## 4. 機能仕様

### 4.1 フォルダ管理

- 作成: 任意のフォルダ配下に新規フォルダを作成する. 階層の深さに制限を設けない.
- 移動: フォルダおよび学習セットを別フォルダへ移動する. 移動先が自身または自身の子孫である場合は拒否し, その旨を表示する.
- 名称変更, 削除を行う.
- 削除時の確認: 配下に学習セットまたは下位フォルダが存在する場合, 「配下の N 件も削除する」か「配下をルートへ移動して当該フォルダのみ削除する」かを選択させる.
- 表示: ホーム画面でツリー表示とし, 各フォルダの開閉状態を保持する. パンくずリストで現在位置を示す.
- ホーム画面の各フォルダ行に「学習セットを追加」「下位フォルダを追加」のボタンを常時表示する. 目的のフォルダを選び直す手間を省くためであり, メニューを開かずに作成できることを優先する.
- **学習セットはドラッグ&ドロップでフォルダ間を移動できる.** 掴む位置を専用のつまみに限定し, 一覧の縦スクロールと競合させない. 判定は Pointer Events で行い, タッチとマウスを同一経路で扱う (6.4 参照).
- フォルダ自体の移動はドラッグ&ドロップではなくメニューから行う. 移動先が自身の子孫でないことの検査を伴い, 落下先として拒否する表現が別途必要になるためである.
- **パンくずリストの中間のフォルダ名をタップすると, そのフォルダの中身 (下位フォルダと学習セット) を一覧するシートを画面下部に表示する.** 本アプリにはフォルダ単位の画面がないため, 画面を離れずに階層を行き来する手段として設ける.
  - 下位フォルダを選ぶとシート内でその中へ移る. 先頭の「← 親フォルダ名」で上の階層へ戻れ, 最上位まで辿れる.
  - 学習セットを選ぶと, どの画面から開いた場合でもセット詳細 (S2) へ移動する. 行き先を一定にし, 迷わせないためである.
  - 一覧では, 現在開いている学習セットと, そこまでの経路上のフォルダを強調する. 各学習セットにはカード枚数を添える.
  - 「ホーム」は従来どおりホーム画面への移動とし, 末尾の現在地 (学習セット名) は操作の対象としない.
  - 画面幅の広い環境ではシートではなく中央のダイアログとして表示する.
- フォルダの ⋯ メニューに「フォルダをコピー」を置く. 下位フォルダと学習セットを階層を保ってまとめて複製し, 最上位の名称を「(元の名称) のコピー」として元と同じ階層に置く. 学習セットの複製は 4.9.1 の既定と同じく, ★とリッチコンテンツ設定を引き継ぎ, 進捗は引き継がない.

### 4.2 学習セット管理

- 作成, 名称変更, 説明文の編集, 削除, フォルダ間の移動を行う.
- **名称と説明文はカード編集画面 (S3) で編集する.** セットの中身を直す操作を1つの画面にまとめ, 「編集は中身, 設定は学習時の振る舞い」という区別を保つためである.
- **名称変更, 別フォルダへの移動, コピー, 統合, 分割, 削除は ⋯ メニューから行う** (4.9 参照). フォルダの ⋯ メニューと同じ並びに揃える.
- セット設定画面 (S10) はリッチコンテンツの有効化のみを扱う (4.4 参照). 扱う項目が生じる段階6 まで, この画面と入口は設けない.
- セット詳細画面に進捗サマリを表示する (習得済 N 枚 / 学習中 M 枚 / 未学習 L 枚, および習得率のバー).
- セット削除時は, 配下のカード, 進捗, 画像を連動して削除する. 確認ダイアログを必須とし, 画像を含む場合は解放される容量を併記する.

### 4.3 カード管理

- 手動入力: 用語, 定義, ヒントを入力して1枚ずつ追加する. 連続追加を想定し, 保存後は入力欄をクリアして同画面に留まる.
- 編集, 削除を行う. 削除時は当該カードの進捗および画像も削除する.
- 並べ替え: ドラッグによる手動並べ替え, および「用語の昇順」「作成日時順」への一括整列を提供する.
- 一覧表示: 用語と定義を2列で表示し, ★の状態をアイコンで示す. 画像を含むカードにはサムネイル (高さ32ピクセル) を添えて示す.
- テキストインポート (4.5) への導線は, カード編集画面 (S3) に「インポート」として主要動作の体裁で置く. カードを増やす操作をこの画面に集約するためである.
- カードの追加後は入力欄を空に戻す. 日本語入力の変換が確定していない場合は確定を待ってから保存し, 未確定の文字列が欄に残らないようにする.

### 4.4 リッチコンテンツ (画像・数式)

学習セット単位で有効・無効を切り替える. 用語集のように文字だけで足りるセットに不要な機構を持ち込まないことを目的とする.

#### 4.4.1 有効化の切り替え

セット設定画面 (S10) に「画像を使う」「数式を使う」の2つのトグルを置く. いずれも新規セットでは無効とする.

無効から有効への切り替えは即時に反映する. **有効から無効への切り替え時**, 当該セットに画像または数式を含むカードが存在する場合は確認ダイアログを表示する.

> このセットには画像 N 枚 / 数式を含むカード M 枚があります. 無効にしても**データは削除されません**が, 画像は表示されなくなり, 数式は入力した LaTeX の文字列がそのまま表示されます. 再び有効にすれば元に戻ります.

データを保持したまま表示だけを切り替える方式とし, 誤操作によるデータ喪失を構造的に防ぐ.

#### 4.4.2 画像

**添付できる位置**

1枚のカードにつき, 表面 (用語側) と裏面 (定義側) にそれぞれ最大1枚を添付する. 画像とテキストは併存でき, 画像のみのカードも作成できる.

**入力経路**

| 経路 | 対象 | 備考 |
|---|---|---|
| ファイル選択 | PC / iPhone | iPhone ではカメラ撮影とフォトライブラリを選べる |
| クリップボードからの貼り付け | 主に PC | `paste` イベントを捕捉する. スクリーンショットを直接貼れる |
| ドラッグ&ドロップ | PC | 入力欄への投下で添付する |

対応形式は PNG, JPEG, WebP, GIF (静止画として扱う), HEIC (ブラウザがデコードできる場合) とする.

貼り付けとドロップは, 画像欄そのものに対して行った場合はその面に付ける. カードの入力欄で貼り付けた場合は, **直前まで触っていた欄に対応する面**に付ける (定義を書きながら貼れば裏面, それ以外は表面). 入力を中断せずにスクリーンショットを貼れるようにするためである.

画像はカードの保存時に書き込む. 添付した時点で書き込むと, 編集をやめたときに参照されない画像が残るためである.

**取り込み時の自動圧縮 (必須)**

無加工の写真は1枚あたり3〜5 MB に達し, 数十枚で端末の保存領域を圧迫する. そのため取り込み時に必ず次の処理を行う.

1. `<canvas>` へ描画し, 長辺が `imageMaxEdge` (既定1600ピクセル) を超える場合は縦横比を保って縮小する.
2. `canvas.toBlob()` で WebP (品質0.82) として再符号化する. WebP 符号化に対応しない環境では JPEG (品質0.85) にフォールバックする.
3. 結果が500 KB を超える場合, 品質を0.1刻みで下げて再試行する (下限0.5).
4. 圧縮前後のバイト数を編集画面に表示する.

この処理により1枚あたり概ね100〜300 KB に収まり, 500枚を添付しても100 MB 程度に留まる.

**表示**

| 場面 | 表示方法 |
|---|---|
| カード一覧 | 高さ32ピクセルのサムネイル |
| カード編集 | 幅に収めたプレビューと削除ボタン |
| 暗記モード | カード中央に配置. 高さは画面の50%を上限とする. タップで全画面オーバーレイに拡大し, ピンチ操作で拡大縮小できる |
| 4択モード (問題文) | 高さ200ピクセルを上限とする |
| 4択モード (選択肢) | 高さ80ピクセルのサムネイル. テキストがある場合はその横に並べる |

4択の誤答は同一セットの答え側の文言から選ぶため (4.7.2), 選択肢に添える画像は**その文言を持つカードの画像**を用いる. 同じ文言のカードが複数ある場合は最初の1枚を採る. 答えが空のカードは出題しないため (4.7.2), 画像だけのカードは4択には現れない.

**テキストインポートとの関係**

4.5 のテキストインポートでは画像を扱えない. 画像はインポート後にカード編集画面から個別に添付する.

#### 4.4.3 数式

**記法**

LaTeX を用いる. `$ ... $` をインライン数式, `$$ ... $$` をブロック数式として解釈する. 描画には KaTeX を用いる.

**解釈の範囲**

数式が**有効なセットのカードに限り**, 用語・定義・ヒントの各テキストを解析して数式を描画する. 無効なセットでは一切解析せず, プレーンテキストとして表示する.

この設計には副次的な利点がある. 数式を使わないセットで `$` を含む文章 (金額表記など) を書いても, 数式として誤って解釈されることがない.

**入力補助**

数式が有効なセットのカード編集画面に, 入力欄の下部でリアルタイムに描画結果を表示する. 構文誤りがある場合は該当箇所を赤字で示す. `\frac{}{}`, `\sum`, `\int`, 上付き・下付きといった頻用記法をボタンで挿入できるパレットを添える.

**描画失敗時の扱い**

KaTeX が解析に失敗した場合, 例外を投げずに元の LaTeX 文字列を等幅フォントの赤字で表示する (KaTeX の `throwOnError: false`). 学習中に画面が停止することを防ぐ.

**読み込みの時機**

KaTeX は本体と字形で300 KB 近くあるため, アプリの起動時ではなく**数式が有効なセットの画面を開いた時点**で読み込む. 文字だけのセットしか使わない利用者に, この重さを負わせないためである. 読み込みが済むまでの一瞬は LaTeX の文字列がそのまま見えるが, 済み次第すぐに組み直す.

#### 4.4.4 セット操作におけるフラグの継承

| 操作 | 継承規則 |
|---|---|
| コピー | 元セットの設定をそのまま引き継ぐ. 画像は複製する |
| 分割 | 元セットの設定をすべての分割先に引き継ぐ. 画像は分割先へ移動または複製する |
| 統合 | 統合元のいずれかで有効なら, 統合先でも有効とする (論理和) |

### 4.5 テキストインポート

Word, Excel, Google ドキュメント等からの貼り付けを想定する.

**入力**

複数行のテキストを貼り付ける入力欄を設ける.

**区切り文字の指定**

| 対象 | 選択肢 | 既定値 |
|---|---|---|
| 用語と定義のあいだ | Tab / カンマ / カスタム文字列 | Tab |
| カードのあいだ | 改行 / セミコロン / カスタム文字列 | 改行 |

**解析規則**

1. カード区切り文字でテキストを分割する.
2. 各要素について前後の空白文字を除去し, 空文字列となったものは破棄する.
3. 用語と定義の区切り文字が1行に複数出現する場合, **最初の1つでのみ分割する**. これにより, 定義側にカンマを含む用語 (例: 「機密性, 完全性, 可用性」) が正しく取り込まれる.
4. 区切り文字が1つも出現しない行は, 用語のみが与えられたものとして扱い, 定義を空にした状態でプレビューに警告表示する.

**プレビュー**

解析結果の先頭20件を表形式で表示し, 総枚数を併記する. 定義が空の行は警告色で示す. 取り込み先セットで数式が有効な場合, プレビューにも数式を描画する.

**取り込み先**

新規セットの作成, または既存セットへの追記を選択する. 「用語と定義が完全に一致するカードを除外する」チェックボックスを設け, 既定でオンとする.

この除外は, 取り込み先セットの既存カードとの重複に加え, **貼り付けたテキスト内での重複**も対象とする. 同じ用語表を誤って二度貼り付ける場面が実際に多いためである. 判定は用語と定義の完全一致で行い, 先に現れたものを残す.

### 4.6 暗記モード

#### 4.6.1 キュー構築

ラウンド開始時に以下の順で対象カードを決定する.

```
候補 ← セット内の全カード
if (starredOnly) 候補 ← 候補のうち starred が true のもの
if (trackProgress) 候補 ← 候補のうち status が 'known' でないもの
if (shuffle) 候補 ← Fisher-Yates シャッフル
else          候補 ← order の昇順
```

候補が0枚となった場合はラウンドを開始せず, 理由 (全カードが習得済である / ★付きカードが存在しない) を表示する.

#### 4.6.2 カード提示と操作

- 表面には `front` で指定した側 (用語または定義) を表示する. 当該面に画像がある場合は画像も表示する.
- カードをタップまたはクリックすると裏返り, 反対側を表示する. 再度タップで戻る. 画像の拡大操作はカードの反転と競合するため, 画像領域のタップは拡大に割り当て, 反転はカードの余白領域に割り当てる.
- 右方向へのスワイプ, または画面下部の緑のチェックボタンで「知っている」と判定する.
- 左方向へのスワイプ, または画面下部の橙のバツボタンで「学習中」と判定する.
- PC では右矢印キーが「知っている」, 左矢印キーが「学習中」に対応する.
- 判定後, 次のカードへ自動的に進む.
- 画面上部に, 当該ラウンドにおける「学習中」件数 (左上) と「知っている」件数 (右上) をカウンタ表示する.
- ヒントが登録されているカードでは左上に「ヒントを見る」を表示し, タップで開示する.
- **学習オプションは暗記モードの画面からも変更でき, 即座に反映する.** 既に判定した分の集計と進捗はそのまま保ち, まだ提示していない残りのカードだけを新しい条件で組み直す. 設定を変えるたびにラウンドが最初に戻ると, 学習の流れが切れるためである.
- **カードは重なりとして描画する.** 現在のカードの背後に次のカードの輪郭を見せ, 内容は見せない. 現在のカードはポインタに追随して持ち上がり, 左右へ払うと画面外へ抜けて, 背後のカードが前面へ繰り上がる. 手で札を繰る感覚に近づけることを意図する.

#### 4.6.3 進捗の更新

`trackProgress` が true の場合, 判定と同時に `CardProgress.status` を更新する (右 → `known`, 左 → `learning`). false の場合は永続化せず, ラウンド内のカウンタのみを更新する.

#### 4.6.4 1つ戻る

直前の1手を取り消す. 取り消し履歴はラウンド単位でスタックに保持し, ラウンドを跨いだ巻き戻しは行わない. 取り消し時は `currentIndex` を1減じ, 当該カードの `status` を判定前の値へ復元する.

#### 4.6.5 ラウンド終了

キューを消化した時点で結果画面 (S7) へ遷移し, 以下を提示する.

- `trackProgress` が true かつ「学習中」が1枚以上: 「学習中の N 枚を続ける」を主要動作とする. 押下すると `learning` のカードのみで次ラウンドを構築する.
- `trackProgress` が true かつ全カードが `known`: 完了表示とする.
- `trackProgress` が false: ラウンド内の集計のみを表示し, 「もう一度」で全候補を再構築する.

#### 4.6.6 最初からやり直す

確認ダイアログを経て, 当該セットの全カードの `status` を `unseen` に戻し, `StudySession` を削除したうえでラウンドを再構築する.

### 4.7 4択モード

#### 4.7.1 出題プール

暗記モードと同様に `starredOnly` を適用できる. さらに「学習中のカードのみ」に絞る選択肢を設ける. 出題順は既定でシャッフルとする. これらは `QuizOptions` (2.7) として記憶する.

#### 4.7.2 問題文と選択肢の生成

- 問題文は `front` で指定した側のテキストおよび画像とする. 正答は反対側の内容となる.
- 誤答は, **同一セット内の他カードの, 正答と同じ側の内容**から重複を除いて無作為に3件抽出する. 重複判定はテキストの完全一致で行い, 画像のみのカードは画像 ID で判定する.
- 抽出可能な誤答が3件に満たない場合, 選択肢数を `誤答数 + 1` に縮小する (最小2択).
- 選択肢が2つも構成できない場合, 当該カードを出題対象から除外する. セット全体でカードが2枚未満の場合は4択モードを開始せず, その旨を表示する.
- 答えとなる側 (または問題文の側) が空のカードは, 正答の選択肢を作れないため出題対象から除外する. テキストインポートで定義が空のまま取り込まれたカード (4.5) が該当する. 除外した枚数は開始時に表示する.
- 選択肢は毎回シャッフルし, 1〜4の番号を付与する.

#### 4.7.3 解答と判定

- 選択肢を選ぶと即座に判定を表示する. 正答は緑, 誤答は赤で示し, 誤答時は正答も併せて強調表示する.
- 「分かりませんか ?」を押すとパスとなり, 正答を表示したうえで誤答として扱う.
- `quizAffectsProgress` が true の場合, **1回の正答で** `status` を `known` に, 誤答およびパスで `learning` に更新する. あわせて `quizCorrect` / `quizWrong` を加算する.
- 画面上部に進捗を `現在の問題番号 / 総問題数` の形式で表示する.

#### 4.7.4 終了と結果

出題プールを1周した時点で結果画面へ遷移し, 正答率, 所要時間, 誤答した問題の一覧 (問題文, 正答, 選択した解答) を表示する. 「間違えた問題だけ再挑戦」および「もう一度」を提供する.

### 4.8 ★マーキング

- カード一覧, カード編集画面, 暗記モードのカード表示上から切り替えられる.
- 学習セット横断ではなくカード固有の属性とする.
- セット詳細画面に「★のみ表示」フィルタを設ける.

### 4.9 学習セットの操作

名称変更, 別フォルダへの移動, コピー, 統合, 分割, 削除は, セット詳細 (S2) の ⋯ メニューと, ホームの学習セットの行の ⋯ メニューから行う. どちらも同じ一覧を開き, 機能は共通とする. 削除は誤操作を避けるため一覧の最後に置く.

#### 4.9.1 コピー

セットを複製する. 名称の既定値は「 (元の名称) のコピー」とする. ★の状態とリッチコンテンツ設定は引き継ぐ. 画像は複製する. 進捗は既定で引き継がず, オプションで引き継ぎを選択できる.

#### 4.9.2 統合

1. 統合するセットを2つ以上選択する.
2. 出力先を選ぶ (新規セットを作成する / 既存セットに追記する).
3. 重複の扱いを選ぶ (そのまま取り込む / 用語と定義が完全一致するカードを除外する. 既定は除外).
4. 統合を実行したのち, 統合元のセットを削除するかどうかを尋ねる. 既定では削除しない. 取り込んだ枚数や除外された枚数を見てから決められるようにするためである.

統合する順番を指定できる. 手順1 ではセットを選んだ順に番号を付け, 手順2 でドラッグして並べ替える. 新規セットへ出力する場合は, この順にカードを連結する. 既存セットへ追記する場合, 追記先は統合するセットの中から選び, 追記先のカードを先頭に今の並びのまま残して, 他のセットを番号の順に後ろへ続ける. セット詳細から始めた場合は, そのセットが1番として選ばれた状態で始まる.

リッチコンテンツ設定は統合元の論理和とする. 統合後のカードの進捗は `unseen` から開始する.

#### 4.9.3 分割

分割基準を以下の3つから選ぶ.

| 基準 | 挙動 | 生成されるセット名 |
|---|---|---|
| 枚数指定 | 現在の並び順で N 枚ずつに分割する | (元の名称) 1, (元の名称) 2, … |
| ★の有無 | ★付きとそれ以外の2つに分割する | (元の名称) ★, (元の名称) その他 |
| 手動選択 | チェックしたカードを新セットへ切り出す | 入力した名称 |

共通オプションとして, 元セットを残すか, 元セットから該当カードを移動 (削除) するかを選ぶ. 出力先フォルダを指定できる. 分割後のカードは元の進捗とリッチコンテンツ設定を引き継ぐ.

移動を選んだ結果, すべてのカードが元セットから出ていく場合 (枚数指定や★の有無による分割) は, 空になった元セットを削除する. 空のセットを残しても使い道がないためである.

### 4.10 全セット横断検索

- 検索対象は用語, 定義, ヒント, および学習セット名とする.
- 数式は**入力した LaTeX ソース文字列**を検索対象とする. 描画後の見た目に対する検索は行わない.
- 照合は部分一致とし, 比較前に次の正規化を順に適用する.

  1. **NFKC 正規化** — 全角英数を半角に, 半角カナを全角カナに揃える (例: 「ＤＮＳ」→「DNS」, 「ﾈｯﾄﾜｰｸ」→「ネットワーク」).
  2. **小文字化** — 大文字と小文字の差異を吸収する (例: 「dns」で「DNS」が一致する).
  3. **カタカナのひらがな化** — Unicode の U+30A1 〜 U+30F6 の文字コードから 0x60 を減じてひらがなに変換する. これにより「ねっとわーく」でも「ネットワーク」が一致する. 長音符 (U+30FC) はそのまま残す. 対応するひらがなが存在する U+30F4 (ヴ) は U+3094 (ゔ) へ変換し, 対応が存在しない U+30F7 〜 U+30FA (ヷヸヹヺ) は変換対象から除外する.

  この順序が重要である. NFKC を先に適用することで, 半角カナがいったん全角カナに揃い, その後のひらがな化が正しく働く.

- 検索結果には, 一致箇所を強調したカード内容と, 所属する学習セット名およびフォルダの階層パスを表示する.
- 結果をタップすると当該セットの当該カードへ移動する.
- 検索対象を特定のフォルダ配下に限定するフィルタを設ける.
- 学習セット名の一致は, カードの一致とは分けて先頭に一覧する.
- 一致したカードが多い場合は先頭の200件を表示し, 一致した総数を併記する. 1文字で検索して数千件が一致しても, 描画が滞らないようにするためである.
- ホーム画面の上部に検索の入口を置く.

### 4.11 バックアップ (エクスポート / インポート)

**エクスポート**

「画像を含める」チェックボックスにより出力形式が変わる. 既定はオンとする.

| 設定 | 形式 | 内容 |
|---|---|---|
| 画像を含める | ZIP | `data.json` と `assets/<id>.webp` を格納する |
| 画像を含めない | JSON | `data.json` 単体. 画像参照は保持したまま実体を省く |

JSON には, フォルダ構造, 学習セット (リッチコンテンツ設定を含む), カード, 進捗, アプリ設定, およびスキーマバージョンを格納する. ファイル名は `flashcards_YYYYMMDD_HHmm.zip` (または `.json`) とする.

画像を Base64 として JSON に埋め込む方式は採らない. データ量が約1.37倍に膨らみ, 数十 MB の単一 JSON をパースする際に端末が停止するおそれがあるためである.

iOS では共有シートを経由してファイルアプリまたは OneDrive へ保存できる.

**インポート**

ZIP または JSON を読み込む. 取り込み方式を2つ提供する.

- 置き換え: 既存データをすべて破棄して復元する. 確認ダイアログを必須とする.
- マージ: 既存データを残したまま追加する. ID が衝突した場合は取り込み側に新しい ID を採番し, フォルダ階層とカード・画像の対応関係を保ったまま結合する.

画像を含まない JSON を取り込んだ場合, 参照先を失った画像は「画像なし」として扱い, 影響を受けたカード数を取り込み後に報告する.

アプリ設定は**置き換えのときだけ**復元する. マージで今の設定を上書きすると, 別の端末のテーマや既定値が黙って持ち込まれることになるためである.

中断状態 (2.6) は取り込みで持ち越さない. 保存されたキューは取り込み前の ID を指しており, 復元しても中身と食い違うためである.

### 4.12 設定

テーマ (ダーク / ライト / システム追従), ショートカットキーの表示有無, 4択の結果を進捗へ反映するか, 画像リサイズの長辺上限, バックアップの実行, 最終バックアップ日時を扱う.

あわせて**使用容量**を表示する. `navigator.storage.estimate()` に対応する環境では総使用量と割当量を示し, 非対応環境では画像バイト数の合計のみを示す. 学習セットごとの画像容量の内訳も一覧できるようにする.

---

## 5. 操作方法の一覧

### 5.1 暗記モード

| 操作 | タッチ | キーボード |
|---|---|---|
| 知っている | 右へスワイプ / 緑のチェック | → |
| 学習中 | 左へスワイプ / 橙のバツ | ← |
| カードを裏返す | カードの余白をタップ | Space または ↑ |
| 画像を拡大 | 画像をタップ | I |
| 1つ戻る | 戻るアイコン | Backspace |
| シャッフル切替 | シャッフルアイコン | S |
| ヒント表示 | 「ヒントを見る」 | H |
| 終了 | 閉じるボタン | Esc |

### 5.2 4択モード

| 操作 | キーボード |
|---|---|
| 選択肢の選択 | 1 / 2 / 3 / 4 |
| 次の問題へ | Enter |
| 終了 | Esc |

### 5.3 カード編集

| 操作 | キーボード |
|---|---|
| 画像を貼り付け | Ctrl + V (入力欄にフォーカスがある状態) |
| 保存して次のカードへ | Ctrl + Enter |

---

## 6. 非機能要件

### 6.1 オフライン動作

初回の読み込み以降, 通信がなくても全機能が動作すること. 学習データは IndexedDB にあるため, アプリの殻 (HTML, JavaScript, CSS, 字形) を Service Worker で先に取り込んでおく.

数式の字形は woff2 のみを先読みの対象とする. 同じ字形の woff と ttf まで抱えると初回の取得が1 MB 近く増えるうえ, 現行のブラウザはいずれも woff2 を選ぶためである.

**更新は自動で適用しない.** 新しい版を見つけたら画面下部に知らせを出し, 利用者が「更新する」を押したときだけ入れ替える. 学習の最中に読み込み直すと, めくっている札や解答中の問題が失われるためである.

### 6.2 データ保全に関する注意事項

iOS の Safari では, スクリプトが書き込んだストレージは7日間アクセスがない場合に消去され得る. ただし**ホーム画面に追加した PWA は Safari 本体とは別に計数される**ため, この制約の実質的な対象外となる. それでも端末の初期化やアプリ削除でデータは失われるため, 以下の運用を仕様に組み込む.

- 初回起動時に「ホーム画面に追加して利用する」ことを案内する.
- 起動時に `navigator.storage.persist()` を要求し, 保存領域の永続化を試みる.
- 最終バックアップから14日以上経過した場合, ホーム画面にバックアップを促すバナーを表示する.
- 使用容量が割当量の80%を超えた場合, 設定画面と ホーム画面に警告を表示する.

### 6.3 性能

- カード10,000枚を保持した状態で, セット詳細画面の初期表示を1秒以内とする. カード一覧は仮想スクロールを用いる.
- 横断検索の応答を, カード10,000枚に対して300ミリ秒以内とする. 正規化済みの検索用文字列をカード保存時にあらかじめ生成して保持し, 検索のたびに正規化を行わない.
- 画像は表示範囲に入ったものだけを `createObjectURL` で展開し, 範囲外になった時点で解放する. 一覧画面で数百枚の画像を同時に保持しない.

**想定データ量**

| 内容 | 1件あたり | 10,000枚 |
|---|---|---|
| テキストのみのカード | 約200 バイト | 約2 MB |
| 圧縮後の画像 | 約200 KB | 500枚で約100 MB |

テキストのみであれば容量は問題にならない. 画像は自動圧縮を前提としても支配的な要素となるため, 6.2 の容量監視を必須とする.

### 6.4 UI

- 既定はダークテーマとする.
- 画面幅375ピクセルから対応する. iPhone ではセーフエリアを考慮した余白を確保する.
- スワイプ判定は Pointer Events で実装し, タッチとマウスを同一経路で扱う.
- カードの反転および左右への離脱にアニメーションを付す. `prefers-reduced-motion` が指定されている場合はアニメーションを抑制する.
- 画像の読み込み中はカードの高さが変動しないよう, `Asset` に保持した幅と高さから縦横比を先に確保する.
- 主要な操作にはアイコンを文言と併記する (設定は歯車, 編集は鉛筆, 削除はごみ箱など). 文字だけの一覧では目的の操作を探す時間が長くなるためである. アイコンは外部の字形に依存せず, 配色を追随させられるよう SVG を同梱する.
- 2値の設定はトグルスイッチで表す. 現在どちらの状態にあるかを, 文言を読まずに判別できるようにするためである.
- **「戻る」は行き先を決め打ちにせず, 直前に見ていた画面へ帰る.** 同じ画面に複数の入口があるためである. たとえば統合画面はホームの ⋯ からもセット詳細の ⋯ からも開けるが, 常にセット詳細へ帰る作りでは, ホームから来た場合に来た道と食い違う. URL を直接開いた場合など戻り先がないときに限り, その画面に応じた既定の画面へ向かう. 行き先を明示したボタン (「ホームへ戻る」「セット詳細へ戻る」など) はこの限りでない.

---

## 7. 技術構成 (案)

| 層 | 採用 | 選定理由 |
|---|---|---|
| 言語 | TypeScript | データモデルが複数テーブルに跨るため, 型による整合性検査の効果が大きい |
| UI | React | 情報量が多く, 状態遷移を伴う画面が中心であるため |
| ビルド | Vite | 設定が軽量で, PWA プラグインが整備されている |
| DB | Dexie.js | IndexedDB の薄いラッパ. 索引付きクエリ, Blob の格納, スキーマ移行を簡潔に記述できる |
| 状態管理 | Zustand | 学習セッションの状態のみを扱えばよく, Redux ほどの機構を要さない |
| ルーティング | React Router (HashRouter) | GitHub Pages はサーバ側の書き換え設定を持たないため, ハッシュ方式が確実に動作する |
| 数式描画 | KaTeX | MathJax より軽量かつ高速で, 同期的に描画できるためオフライン用途に適する |
| ZIP 生成 | fflate | JSZip より小さく高速. エクスポート / インポートの双方に用いる |
| PWA 化 | vite-plugin-pwa | Service Worker とマニフェストの生成を自動化する |
| 公開 | GitHub Pages + GitHub Actions | `main` への push で自動ビルド・自動公開する |

ロジック層 (データアクセス, キュー構築, 選択肢生成, インポート解析, 検索正規化, 画像圧縮) を UI から分離して実装する. 将来 React Native へ移行する場合, この層をそのまま流用できる.

---

## 8. 開発フェーズ

| 段階 | 内容 | 完了条件 |
|---|---|---|
| 0 | GitHub リポジトリの作成, 開発環境の構築, GitHub Pages への自動公開の設定 | 空のページが GitHub Pages の URL で表示され, iPhone からも開ける |
| 1 | データ層とフォルダ / セット / カードの CRUD | カードを作成・編集・削除でき, 再読み込み後も保持される |
| 2 | テキストインポート | 応用情報の用語表を貼り付けてセットを生成できる |
| 3 | 暗記モードと進捗管理 | 振り分け, 循環, 1つ戻る, 各オプションが動作する |
| 4 | 4択モード | 選択肢生成と結果画面が動作する |
| 5 | セット操作と横断検索 | コピー, 統合, 分割, 検索が動作する |
| 6 | リッチコンテンツ | セット単位の有効化, 画像の添付と圧縮, 数式の描画が動作する |
| 7 | PWA 化とバックアップ | ホーム画面から起動し, 機内モードで学習でき, ZIP の書き出しと読み込みができる |

段階0を最初に置くのは, 早い段階から実機で動作を確認できるようにするためである. GitHub Pages への公開経路を先に通しておけば, 以降は変更を push するたびに iPhone で確認できる.

段階3を終えた時点で実用に足るため, そこから実際の試験勉強に投入し, 使用感を踏まえて以降の仕様を調整することを推奨する.

---

## 9. 決定事項と残件

### 9.1 確定した事項

| # | 項目 | 決定 |
|---|---|---|
| D1 | 公開先 | GitHub Pages. ソース管理を兼ねる. 手順は別途の入門ガイドで詳述する |
| D2 | 4択の習得判定 | 1回の正答で `known` とする |
| D4 | 検索のかな折り畳み | ひらがなとカタカナを同一視する (4.10 参照) |
| D5 | リッチコンテンツ | 画像と数式に対応し, 学習セット単位で有効化する (4.4 参照) |

### 9.2 残件

| # | 項目 | 現状の設計 |
|---|---|---|
| D3 | 統合時の進捗の扱い | 引き継がず `unseen` から開始する. 引き継ぎが必要であれば変更する |

---

## 10. v1 のスコープ外

以下は今回作成しない. 必要が生じた時点で改めて検討する.

- クラウド同期, アカウント機能, 複数端末でのリアルタイム共有
- 読み上げ音声 (TTS)
- 1つの面に複数枚の画像を添付すること
- 音声・動画の添付
- 手書き入力
- 間隔反復 (SRS) による復習日の自動算出
- 記述式テスト, 並べ替え問題などの追加出題形式
- 学習セットの他者との共有, 公開
- 学習時間の統計, ヒートマップ等の可視化
