# 引き継ぎ手順書 — Cowork から Claude Code への移行

作成日: 2026-09-10

この手順書は, 仕様策定までを終えたプロジェクトを Claude Code で実装するために, 利用者自身が行う操作をまとめたものである. 所要時間は初回で30〜45分程度を見込む.

---

## 0. 全体の流れ

| 段階 | 作業場所 | 内容 |
|---|---|---|
| 1 | ブラウザ | Node.js と Git for Windows をインストールする |
| 2 | エクスプローラー | OneDrive の外に作業用フォルダを作る |
| 3 | ブラウザ ( GitHub ) | リポジトリを作成する |
| 4 | PowerShell ( または VS Code ) | リポジトリをローカルに複製する |
| 5 | エクスプローラー | 引き継ぎファイルを配置する |
| 6 | Claude Code ( または PowerShell ) | コミットして GitHub へ送る |
| 7 | Claude デスクトップアプリ | Code タブでフォルダを開く |
| 8 | Claude Code | 最初の指示を出す |

---

## 1. 事前準備

### 1.1 Node.js

Vite ( ビルドツール ) と npm ( パッケージ管理 ) を動かすために必要である.

1. https://nodejs.org/ja を開く.
2. **LTS** と表示されている方をダウンロードする ( Current ではない ). LTS は長期サポート版を意味し, 安定している.
3. インストーラを実行する. 設定は既定のままでよい.
4. 確認: スタートメニューで「PowerShell」と検索して起動し, 次を入力して Enter を押す.

   ```powershell
   node -v
   npm -v
   ```

   `v24.x.x` のようなバージョン番号が2つ表示されれば成功である.

### 1.2 Git for Windows

**Claude Code のローカルセッションは Windows 上で Git を必要とする.** GitHub Desktop にも Git は同梱されているが, それは GitHub Desktop の内部専用で, 他のプログラムからは見えない場所にある. そのため別途インストールする.

1. https://git-scm.com/downloads/win を開き, 64-bit 版をダウンロードする.
2. インストーラを実行する. 選択肢が多く表示されるが, **すべて既定のままで問題ない**. ひたすら「Next」を押してよい.
3. 確認: PowerShell で次を入力する.

   ```powershell
   git --version
   ```

   `git version 2.xx.x.windows.1` のように表示されれば成功である.

### 1.3 Claude デスクトップアプリ

**すでにインストール済みである.** 現在この会話を行っているアプリがそれであり, 上部の「Code」タブに切り替えるだけで Claude Code が使える. 追加のインストールは不要である.

---

## 2. 作業用フォルダを作る ( エクスプローラー )

### なぜ OneDrive の外に置くのか

`npm install` を実行すると `node_modules` というフォルダが作られ, ここに**数万個のファイル**が展開される. これが OneDrive の同期対象にあると, 次の問題が起こる.

- 同期処理が延々と続き, PC が重くなる.
- 同期中のファイルがロックされ, `npm install` やビルドが途中で失敗する.
- OneDrive の容量を無駄に消費する.

### なぜ全角文字を含まないパスに置くのか

Windows のユーザ名が `坂口凜生` であるため, ユーザーフォルダ配下のパスには全角文字が含まれる. 現在の Node.js と Vite は UTF-8 のパスを問題なく扱うが, 一部のビルドツールやネイティブモジュールが全角パスで不可解な失敗を起こす事例が残っている. あらかじめ半角のみのパスに置くことで, この種の障害を回避する.

### 置き場所として避けるべき場所

| 場所 | 理由 |
|---|---|
| OneDrive 配下 | `node_modules` の同期でビルドが破綻する |
| `C:\Users` の直下 | ユーザープロファイル専用のシステムディレクトリである. 管理者権限で作成すると所有者が Administrators になり書き込めなくなることがあり, 「dev という名前のユーザーアカウント」に見えるためツール類の誤認も招く |
| `ドキュメント`, `デスクトップ`, `ピクチャ` | OneDrive にリダイレクトされている場合がある |

### 手順

1. エクスプローラーを開く.
2. アドレスバーに `C:\` と入力して Enter を押す.
3. 「新規作成」→「フォルダー」を選び, 名前を **`dev`** とする. 管理者権限の確認が表示されたら承認する.

以降, プロジェクトは `C:\dev\flashcard-app` に置く. 半角のみで構成され, OneDrive とも無関係である.

> 作成後に書き込めない場合は, `C:\dev` を右クリック →「プロパティ」→「セキュリティ」で自分のユーザーに「フルコントロール」を付与する.

> **補足**: プロジェクトを移しても npm のキャッシュは `C:\Users\坂口凜生\AppData\` 配下に残る. 実害が出ることはまずないが, キャッシュ関連で不可解なエラーが生じた場合は `npm config set cache C:\dev\npm-cache` で移せる.

---

## 3. GitHub でリポジトリを作る ( ブラウザ )

1. https://github.com にログインする.
2. 右上の「+」→「New repository」を選ぶ.
3. 次のように入力する.

   | 項目 | 入力内容 | 理由 |
   |---|---|---|
   | Repository name | `flashcard-app` | 半角英小文字とハイフンのみを推奨. **この名前が公開 URL に入るため, 後から変えないこと** |
   | Description | `応用情報技術者試験対策の単語帳アプリ (PWA)` | 任意 |
   | 公開設定 | **Public** | 後述 |
   | Add a README file | **チェックを入れる** | 空のリポジトリだと複製できないため |
   | Add .gitignore | **None のまま** | こちらで用意したものを使う |
   | Choose a license | **None のまま** | 個人用のため不要 |

4. 「Create repository」を押す.

### Public を選ぶ理由

無料の GitHub アカウントでは, **GitHub Pages は Public リポジトリでしか使えない**. Private リポジトリで Pages を使うには GitHub Pro 以上の有料プランが必要である.

このアプリは公開されて困るものではない. 学習データはすべて端末内 ( IndexedDB ) に保存され, リポジトリには一切含まれないため, 自分の学習内容が他人に見えることもない.

なお **学生は GitHub Education の認証を通すと GitHub Pro が無料になる**. その場合は Private でも Pages が使えるので, 後から切り替えることもできる.

### 公開後の URL

作成したリポジトリの公開 URL は次のようになる.

```
https://<あなたのGitHubユーザ名>.github.io/flashcard-app/
```

**この URL の形式が重要である.** サイトがサブディレクトリ配下に置かれるため, Vite の設定 ( `base` ) をリポジトリ名に合わせる必要がある. これを忘れると公開後に真っ白なページになる. `CLAUDE.md` に注意書きを入れてあるので, Claude Code 側が対応する.

---

## 4. リポジトリを複製する ( クローン )

GitHub 上のリポジトリを手元の PC に複製する. 履歴ごと複製されるため, これ以降は手元でコミットを積める.

### 推奨: PowerShell を使う

GitHub Desktop は不要である. コマンドの意味がそのまま残るため, 学習目的にはこちらが向く.

1. スタートメニューで「PowerShell」を検索して起動する.
2. 次を1行ずつ実行する ( `<ユーザ名>` は自分の GitHub ユーザ名に置き換える ).

   ```powershell
   cd C:\dev
   git clone https://github.com/<ユーザ名>/flashcard-app.git
   ```

   `cd` は作業ディレクトリの移動, `git clone` は複製である. リポジトリの URL は GitHub のページの緑色の「Code」ボタンから取得できる.

3. 確認する.

   ```powershell
   cd C:\dev\flashcard-app
   dir
   ```

   `README.md` が表示されれば成功である. `.git` という隠しフォルダも作られるが, これが履歴の実体なので触らないこと.

### 代替: VS Code を使う

`Ctrl + Shift + P` →「Git: Clone」→ URL を貼り付け → 保存先に `C:\dev` を選ぶ.

### 代替: GitHub Desktop を使う

「File」→「Clone repository」→「GitHub.com」タブで `flashcard-app` を選ぶ.

**Local path の既定値は `C:\Users\坂口凜生\Documents\GitHub` になっていることが多い. Documents は OneDrive に同期されている可能性があるため, 必ず `C:\dev` に変更する.** 最終的に `C:\dev\flashcard-app` になっていることを確認してから「Clone」を押す.

---

## 5. 引き継ぎファイルを配置する ( エクスプローラー )

この会話で受け取った次のファイルを, `C:\dev\flashcard-app` にコピーする.

| ファイル | 役割 |
|---|---|
| `specs.md` | 仕様書 ( 正本 ) |
| `CLAUDE.md` | Claude Code が毎回自動で読む開発規約 |
| `.gitignore` | git に記録させないファイルの指定 |
| `README.md` | リポジトリの説明. **既存のものを上書きする** |
| `GITHUB_SETUP.md` | この手順書 |

`README.md` は手順3で GitHub が作ったものが既にあるため, 「置き換える」を選んで上書きする.

### `.gitignore` について

先頭がピリオドで始まる特殊な名前のファイルである. エクスプローラーで新規作成しようとすると名前を受け付けないことがあるが, **今回はファイルとして渡してあるのでコピーするだけでよい.**

このファイルは「git に記録しないもの」の一覧である. `node_modules` ( 数万ファイル ) や `dist` ( ビルド結果 ) は, いつでも再生成できるため記録する必要がなく, 記録するとリポジトリが肥大化する.

---

## 6. コミットして GitHub へ送る

### 推奨: Claude Code に依頼する

手順7でフォルダを開いた後, 最初の指示 ( 手順8 ) の中でコミットまで依頼すればよい. `CLAUDE.md` に「コマンドとその意味を説明してから実行する」と規約を入れてあるため, 何が起きているかを確認しながら進められる.

### 自分で実行する場合

```powershell
cd C:\dev\flashcard-app
git status
git add .
git commit -m "docs: 仕様書と開発規約を追加"
git push
```

| コマンド | 意味 |
|---|---|
| `git status` | 現在の変更状況を確認する. 迷ったらまずこれを打つ |
| `git add .` | 変更を「コミットに含めるもの」として選ぶ ( ステージング ). `.` は現在位置以下すべてを指す |
| `git commit -m "..."` | 選んだ変更に説明をつけて履歴に刻む. **この時点ではまだ自分の PC の中だけの操作である** |
| `git push` | 手元のコミットを GitHub のサーバへ送る. ここで初めてインターネットから見えるようになる |

**初回の `git push` ではブラウザが開き, GitHub のサインインを求められる.** Git Credential Manager による正常な動作で, 一度通せば以降は記憶される. GitHub は2021年にパスワード認証を廃止しているため, 仮に「Username / Password」を尋ねるプロンプトが出てもパスワードでは通らない. ブラウザ経由の認証を使うこと.

終わったらブラウザでリポジトリを開き直し, ファイルが並んでいれば成功である.

### コミットとプッシュが分かれている理由

手元で何度もコミットしてから, まとめて送れるようにするためである. 電車の中でオフラインでもコミットはできる, と考えるとわかりやすい.

| 用語 | 意味 |
|---|---|
| **origin** | 送り先のサーバの通称. クローン元の GitHub リポジトリを指す |
| **main** | 既定のブランチ名. 「幹」にあたる作業線である |

---

## 7. Claude Code でフォルダを開く

1. **Claude デスクトップアプリ**を開く ( この会話をしているアプリである ).
2. 画面上部中央の **「Code」タブ**をクリックする.
3. 環境の選択で **「Local」** を選ぶ. 自分の PC 上のファイルを直接扱うモードである.
4. **「Select folder」** を押し, `C:\dev\flashcard-app` を選ぶ.
5. モデルを選ぶ ( 既定のままでよい ).
6. 権限モードを選ぶ. 送信ボタンの隣にある選択欄である.

   | モード | 挙動 | 推奨 |
   |---|---|---|
   | Manual | 変更のたびに承認を求める | **最初はこれを推奨.** 何が起きているか観察できる |
   | Accept edits | ファイル編集を自動承認する | 慣れてから |
   | Auto | 危険な操作のみ自動で止める | 慣れてから |
   | Plan | ファイルを変更せず方針だけ提案する | 大きな変更の前に |

   慣れるまでは Manual で始め, 差分 ( diff ) を確認しながら進めることを勧める.

---

## 8. 最初の指示 ( コピーして貼り付ける )

Code タブの入力欄に, 次をそのまま貼り付けて送信する.

```
このリポジトリで単語帳アプリを開発します。まず CLAUDE.md と specs.md を読んでください。

今回のゴールは specs.md の §8 に定めた「段階0」です。

1. Vite + React + TypeScript でプロジェクトを初期化する
2. vite.config.ts の base を '/flashcard-app/' に設定する
3. main への push で GitHub Pages へ自動公開される GitHub Actions のワークフローを作る
4. 動作確認用に、アプリ名を表示するだけの最小のページを作る

進める際は次をお願いします。

- git の操作を行うときは、実行するコマンドとその意味を先に日本語で説明してください
- GitHub の Web 画面で私が手動で行う必要がある設定があれば、どのメニューを辿るかを具体的に教えてください
- 私は git と Web フロントエンドの実務経験が浅いので、初出の用語には短い説明を添えてください
```

---

## 9. この後 GitHub 側で必要になる設定

段階0の途中で, **GitHub の Web 画面でしかできない設定**が1つある. Claude Code から案内があるはずだが, 先に把握しておくとよい.

1. ブラウザで自分のリポジトリを開く.
2. 上部の **「Settings」** タブをクリックする.
3. 左側のメニューから **「Pages」** を選ぶ.
4. 「Build and deployment」の **「Source」** を **「GitHub Actions」** に変更する.

これは「公開の材料をどこから取るか」の指定である. 既定は「特定のブランチのファイルをそのまま置く」設定になっているが, 今回は Vite でビルドした結果を公開するため, GitHub Actions からの配信に切り替える必要がある.

---

## 付録A. git 用語集

| 用語 | 意味 |
|---|---|
| リポジトリ ( repository ) | プロジェクト1つ分の入れ物. 変更の履歴をすべて含む |
| クローン ( clone ) | GitHub 上のリポジトリを手元の PC に複製すること. 履歴ごと複製される |
| コミット ( commit ) | 変更内容に説明をつけて履歴に刻むこと |
| プッシュ ( push ) | 手元のコミットを GitHub へ送ること |
| プル ( pull ) | GitHub 側の変更を手元に取り込むこと. 別の端末で作業した後に使う |
| ブランチ ( branch ) | 作業線. 今回は `main` 一本で進める |
| リモート ( remote ) / origin | 送受信先のサーバ. `origin` はクローン元を指す通称 |
| 差分 ( diff ) | 変更前と変更後の違い. 行単位で表示される |
| `.gitignore` | git に記録させないファイルの指定 |
| GitHub Actions | GitHub 上で自動実行される処理. 今回はビルドと公開に使う |

## 付録B. つまずきやすい点

| 症状 | 原因と対処 |
|---|---|
| `npm install` が異常に遅い, 途中で失敗する | プロジェクトが OneDrive 配下にある. `C:\dev` へ移す |
| 公開したページが真っ白, F12 で 404 が並ぶ | `vite.config.ts` の `base` がリポジトリ名と一致していない |
| ページを再読み込みすると 404 になる | BrowserRouter を使っている. HashRouter に変更する |
| Settings に「Pages」が見当たらない | リポジトリが Private かつ無料プランである. Public にするか GitHub Education で Pro を取得する |
| Code タブでフォルダを開けない | Git for Windows が入っていない. 手順1.2 を実施する |
| クローン先を間違えた | 複製したフォルダごと削除し, `C:\dev` へ複製し直すのが確実. GitHub 側には何も影響しない |
| `git status` に大量のファイルが表示される | `.gitignore` が配置されていないか, `node_modules` を作った後に配置した. `.gitignore` を置いてから `git rm -r --cached node_modules` で追跡を外す |
| 初回の `git push` でユーザ名とパスワードを聞かれ, 入力しても弾かれる | GitHub は2021年にパスワード認証を廃止した. ブラウザが開く方式 ( Git Credential Manager ) で認証する. ブラウザが開かない場合は `git config --global credential.helper manager` を実行してから再試行する |
| `git push` で `src refspec main does not match any` と出る | まだ1回もコミットしていない. `git commit` を先に実行する |
| 日本語のコミットメッセージが文字化けする | `git config --global i18n.commitEncoding utf-8` と `git config --global core.quotepath false` を設定する |
